import { RoomValidatorDirective } from './room-validator.directive';

describe('RoomValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new RoomValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
